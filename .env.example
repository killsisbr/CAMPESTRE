# ==============================================================================
# ARQUIVO DE EXEMPLO PARA VARIÁVEIS DE AMBIENTE
# ==============================================================================
# Copie este arquivo para .env e preencha com os dados do seu restaurante
# NUNCA commite o arquivo .env real no git!
# ==============================================================================

# ------------------------------------------------------------------------------
# SERVIDOR
# ------------------------------------------------------------------------------
# Porta do servidor (padrão: 3005)
PORT=3005

# Ambiente (development ou production)
NODE_ENV=development

# ------------------------------------------------------------------------------
# INFORMAÇÕES DO RESTAURANTE
# ------------------------------------------------------------------------------
# Nome do restaurante (exibido nas páginas e mensagens)
RESTAURANT_NAME=Meu Restaurante

# Domínio público do app (sem https://), usado para montar links de pedido
# Exemplo: meurestaurante.com.br ou localhost:3005
APP_DOMAIN=localhost:3005

# ------------------------------------------------------------------------------
# LOCALIZAÇÃO DO RESTAURANTE
# ------------------------------------------------------------------------------
# Coordenadas do restaurante (usadas para calcular distância de entrega)
# Dica: Use Google Maps para obter as coordenadas
RESTAURANT_LATITUDE=-25.4284
RESTAURANT_LONGITUDE=-49.2733

# ------------------------------------------------------------------------------
# APIS EXTERNAS
# ------------------------------------------------------------------------------
# Chave da API OpenRouteService (obrigatória para cálculo preciso de distância)
# Obtenha gratuitamente em: https://openrouteservice.org/dev/#/signup
ORS_API_KEY=sua_chave_ors_aqui

# Chave da API do Google Maps (opcional, para geocoding de endereços)
# Obtenha em: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=sua_chave_google_aqui

# ------------------------------------------------------------------------------
# WHATSAPP
# ------------------------------------------------------------------------------
# ID do grupo do WhatsApp para envio de pedidos
# O ID deve estar no formato: 120363403943975805@g.us
# Para descobrir o ID correto, acesse: http://localhost:3005/api/whatsapp/groups (depois de conectar o WhatsApp)
# Ou use apenas os números, o sistema adiciona @g.us automaticamente
WHATSAPP_GROUP_ID=

# ------------------------------------------------------------------------------
# SEGURANÇA (JWT)
# ------------------------------------------------------------------------------
# Chave secreta para assinar tokens JWT (links de boas-vindas)
# IMPORTANTE: Mude para uma chave forte e única em produção!
JWT_SECRET=change_this_to_a_strong_random_secret_in_production

# Tempo de expiração do token de boas-vindas (padrão: 90d = 90 dias)
WELCOME_TOKEN_EXPIRES=90d

# Intervalo em horas para permitir reenvio de mensagem de boas-vindas (padrão: 12)
WELCOME_RESEND_HOURS=12

# ------------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# ------------------------------------------------------------------------------
# Forçar uso de geolocalização para calcular entrega (true/false)
# Se true, clientes devem usar o botão "Calcular frete" 
ENFORCE_GEOLOCATION_CALCULATION=false